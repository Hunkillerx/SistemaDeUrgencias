<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tibu.sistemadeurgencias">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".vista.ActivitdadPrincipal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".vista.AgregarHospital" />
        <activity android:name=".vista.AgregarAmbulancia" />
        <activity android:name=".vista.MostrarAmbulancias" />
        <activity android:name=".vista.MostrarHospitales" />
        <activity android:name=".vista.MostrarPacientes" />
        <activity android:name=".vista.MostrarAmbulanciasLibres" />
        <activity android:name=".vista.MostrarAmbulanciasOcupadas" />
        <activity android:name=".vista.MostrarInfoHospital"></activity>
    </application>

</manifest>